version: "3"
services:
  fe:
    image: node:latest
    depends_on:
      - be
    volumes:
      - ./fe:/code
    working_dir: /code
    command: sh -c "npm i && npm run serve"
  be:
    image: node:latest
    volumes:
      - ./be:/code
    working_dir: /code
    depends_on:
      - db
    command: sh -c "npm i && npx knex migrate:latest && npx knex seed:run && npm start"
  db:
    image: postgres
    environment:
      - POSTGRES_PASSWORD=password
    expose:
      - "5432"
